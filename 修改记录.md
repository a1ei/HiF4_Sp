给权重做 SmoothQuant 平滑（只平滑不量化）。
增加 NVFP4 权重反量化到 FP 的转换流程。
增加 TACO train split 校准，并按 LiveCodeBench v6 test 题面去重。
调整 TACO 校准文本为 question + starter_code + first_solution，并跳过没有第一份 solution 的样本。

增加 GPTQ A4 激活量化校准：在 Hessian 统计和层间传播时使用量化后的 Linear 输入。
增加 GPTQ 批量校准参数 gptq_calib_batch_size，支持多个定长校准样本并行前向计算。
为 GPTQ、AWQ 和 OmniQuant 增加高熵/低熵 token 重要性方向选择。
调整 OmniQuant 的熵梯度加权，改为基于 Transformer block 输出及其梯度计算 token 重要性，并增加 entropy_grad_norm 模式。
增加 OPD student-forced 校准数据生成与加载流程，支持在 OmniQuant + LFQ 的最后一层使用 OPD 输出 token 进行校准。
增加 lm_head LoRA 软标签蒸馏训练，支持全部 token、逻辑连接词与 </think> 位置范围，并支持 full_lm_head 和 end_think_row 两种适配器范围。
完善 end-think adapter 训练的学生模型激活假量化处理。
新增 A4 校准采样、熵方向对比、GPTQ end-think LoRA 复现、lm_head 蒸馏范围对比和 OPD 熵加权 LFQ 等实验脚本与结果汇总。
更新量化命令脚本、OmniQuant 冒烟测试和项目安装依赖。
